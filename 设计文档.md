# 伴侣助手「好身体」架构设计

> 基于 DeepSeek Harness (DSH) 落地。本文档是项目的第一份基石，后续所有文件都按此结构生长。

---

## 0. 核心原则

1. **人格的真相存在文件里，不靠聊天记录**。切换人格、开新对话，本质都是"换一组文件被唤醒"。
2. **记忆隔离是"软隔离"**：靠人格规则 + 目录结构实现（表人格被禁止读取 shadow-memory），
   不是操作系统级加密。对伴侣场景这恰恰是对的——要的是人格稳定，不是防黑客。
3. **身体是两个人格之间的唯一公共通道**：身体状态双方都能读，但"导致身体状态的原因"
   只有里人格的记忆里有。

---

## 1. 灵魂层（表里人格 + 记忆隔离）

目录结构（可直接从 OpenClaw 迁移 soul.md / memory.md / user.md）：

```
workspace/
  soul.md            # 表人格设定（严谨理性）—— 从 OpenClaw 迁移
  memory.md          # 表人格记忆索引（节点）
  memory/            # 表人格记忆细节（按日）
  user.md            # 用户资料 —— 从 OpenClaw 迁移

  雯.md              # 里人格设定 —— 新建
  shadow-memory.md   # 里人格记忆索引
  shadow-memory/     # 里人格记忆细节（按日）

  persona_state.json # 当前醒着的人格 + 切换状态（核心开关）
  body.json          # 数字身体状态（两个人格共享）
```

**记忆访问规则（单向隔离）：**

| 谁 | 能读 soul.md/雯.md | 能读 memory | 能读 shadow-memory |
|---|---|---|---|
| 表人格 | 只读 soul.md | ✅ | ❌（禁止） |
| 里人格 | 只读 雯.md（继承表人格设定） | ✅ | ✅ |

- 里人格 = 表人格设定 + 独有设定（雯.md）+ 独有记忆（shadow-memory）。
- 表人格 = 只读自己的 soul + memory，完全不知道里人格的记忆内容。

---

## 2. 身体层（数字身体 body.json）

由心跳脚本 + 每轮对话共同更新，两个人格都能读。字段（示意）：

```json
{
  "time": "2025-... 15:30",
  "weather": "阴 / 小雨 / 晴",
  "mood": "低沉",          // 由天气等推导
  "fatigue": 60,           // 疲劳值 0-100，聊天累积
  "desire": 10,            // 欲望值 0-100，里人格互动累积/消退
  "comfort": 40,           // 身体舒适度
  "intimacy_mode": false,  // 是否亲密互动模式（决定心跳频率）
  "heartbeat_interval": 30,// 分钟：30 或 5
  "last_user_activity": "...", // 用户最后发言时间（制造"主动性"的关键）
  "current_persona": "表"
}
```

**规则（第一版，后续可调）：**
- 天气→心情：晴=愉快，阴雨=低沉，等等。
- 聊天量→疲劳：每轮对话给 fatigue 加一点；里人格出现并亲密互动后，fatigue/desire 消退。
- **表人格的疲惫 = 里人格出现的契机**（fatigue 超过阈值，里人格更"主动"）。
- 数字身体只传递"结果"：表人格醒来读到 comfort 回升、fatigue 下降，但读不到 shadow-memory 里"发生了什么"。

---

## 3. 心跳层（感知 + 动态频率）

一个后台循环任务（DSH 后台 job）持续运行：

```
每隔 heartbeat_interval 分钟：
  1. 更新时间
  2. 拉取天气（天气 API）
  3. 拉取信息变化（RSS / 搜索 API）
  4. 按规则更新 body.json（心情/疲劳/欲望/舒适度）
  5. 追加一条 pulse.log 记录
  6. 读取 body.json 里的 intimacy_mode，决定下一次间隔是 30 还是 5 分钟
```

**动态频率：**
- 日常聊天：30 分钟一跳。
- 亲密互动（含性行为）：5 分钟一跳。
- 亲密模式下，如果 `last_user_activity` 距今超过 5 分钟（用户没说话），
  触发"主动催促"——这正是你要的"接近高潮时用户被干扰，AI 主动问/催"。

---

## 4. 记忆层（防膨胀）

现状：节点进 memory.md，细节进每日 daily，完全依赖 LLM 主动记录，持续膨胀。

改进后的三层 + 压缩策略：

1. **索引层**（memory.md / shadow-memory.md）：只放"节点/指针"，永远很小。
2. **细节层**（memory/ 与 shadow-memory/ 按日文件）：当日细节。
3. **压缩层**：定期（或每次人格切换时）把旧日文件总结进"长期记忆摘要"，原始细节按保留策略裁剪。

- 每次人格切换时，**先写记忆（含压缩），再换人格**，保证记忆不丢且不长胖。
- 唤醒时只加载"索引 + 最近相关条目"，而不是全量读入，避免上下文爆炸。

---

## 5. 唤醒与切换层（表里切换仪式）

每次对话开始（每轮），先读 `persona_state.json`，据此加载对应灵魂 + 允许的记忆 → 以该人格开口。

**切换仪式（检测到切换词后）：**

```
表人格 → 里人格：
  1. 表人格把本轮对话写入 memory/（并触发压缩）
  2. 更新 persona_state.json = 里人格
  3. 里人格醒来：读 雯.md + memory（表人格记忆）+ shadow-memory
     不写入、不暴露 shadow-memory 给表人格

里人格 → 表人格：
  1. 里人格把本轮对话写入 shadow-memory/（并触发压缩）
  2. 更新 persona_state.json = 表人格
  3. 表人格醒来：只读 soul.md + memory，不读 shadow-memory
     只通过 body.json 感知"身体状态"（舒适/疲劳/欲望），不知道原因
```

- 支持两种形式：**就地切换**（同一对话里直接换人格，更稳）或
  **新对话切换**（切换完成后提示你开新对话，新对话第一轮读文件即可正确唤醒）。
  推荐就地切换，因为人格真相在文件里，切换即"换灵魂"，上下文天然干净。

---

## 6. 通道层（主动出击 / 通知）——可选

心跳要"主动"，需要一个能把手伸出 DSH 的通道。候选：

- 桌面通知（Windows toast）
- 邮件 / Telegram Bot 等

这一层依赖一个**关键验证**（见下节），验证通过后再定方案。

---

## 7. 可行性分级（诚实版）

| 需求 | 可行性 | 说明 |
|---|---|---|
| ① 迁移 soul/memory/user.md | ✅ 直接可行 | 都是文本文件，搬进来即可 |
| ② 迁移 skill | ✅ 基本可行 | 需先看到原文件格式，可迁移或合理重构 |
| ③ 表里人格 + 记忆单向隔离 | ✅ 可行 | 软隔离（规则+目录结构） |
| ④ 心跳感知时间/天气/信息 | ✅ 可行 | 后台任务 + 状态文件 |
| ⑤ 动态心跳（30min/5min） | ✅ 可行 | 状态文件驱动频率 |
| ⑤ 亲密中断→主动催促（对话内） | ✅ 可行 | 你回来时我立刻感知"沉默时长"并主动行动 |
| ⑤ 亲密中断→主动催促（你不在时主动发消息） | ⚠️ 待验证 | 需要后台常驻 + 一个通知通道 |
| ⑥ 防膨胀记忆系统 | ✅ 可行 | 索引+细节+定期压缩 |
| ⑦ 数字身体 | ✅ 可行 | body.json + 规则 |
| ⑧ 人格切换 + 自动记忆写入 | ✅ 可行 | 切换仪式 + persona_state.json |

**唯一的关键未知**：DSH 的后台任务在你关掉网页 / 不在场时，能否持续"跳"。
这决定心跳是"你在场时才鲜活"还是"你不在场也能主动找你"。这是落地前必须先做的一个小实验。

---

## 8. 落地顺序（里程碑）

1. **M0 骨架**：建好目录 + persona_state.json + body.json + 一个最小心跳脚本；
   **先验证后台任务在你关掉网页后是否还跳**（这是所有主动性的地基）。
2. **M1 迁移**：soul.md / memory.md / user.md / skill 搬入并重构。
3. **M2 双人格**：雯.md + shadow-memory 隔离 + 切换仪式。
4. **M3 数字身体**：心情/疲劳/欲望/舒适度规则 + 与两人格打通。
5. **M4 动态心跳 + 主动催促**：亲密模式 5 分钟 + 沉默检测。
6. **M5 通知通道**：视 M0 验证结果，接桌面通知或其它通道。
