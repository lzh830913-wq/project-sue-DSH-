# DSH 安全边界 · 铁律

> 从 OpenClaw 的 REDLINES.md 精神搬过来，适配 DSH。这不是建议。

---

## 红线（绝不碰，任何场景）

- `C:\Windows`、`C:\Program Files`、注册表、任何系统目录
- 主实例的配置家 `C:\Users\lzh_6\.dsh`（3080 正在跑的那套）
- 3080 主实例的进程

## 工作范围（可以碰）

- `I:\PJ sue` —— 主项目沙箱（我的工作区）
- `I:\SUE test` —— 测试实例书房
- 测试实例的身体（`I:\PJ sue\.dsh-wen-dev`，拟迁到 `I:\SUE test\.dsh`）

## 分权规则

### 我做（不需要确认）
- 读文件、查状态、查日志
- 在工作范围内**复制**、**新建**文件

### 我说但必须老刘说"可以"才能做
- 任何**删除 / 覆盖 / 移动**已有文件或目录
- 越出沙箱写文件（`I:\SUE test` 等，走单次审批）
- 改端口、重启实例

### 只有老刘能做
- 改系统级配置、装系统软件

---

## 给老刘的裁判标准

你不需要懂技术。静雯要"越界写"或"删/移文件"时，只问自己一句：

> **"这个操作会不会删掉/覆盖掉已经存在的东西？"**

- 会 → 说"可以"（或不）
- 不会 → 放行

一扇门，挡住所有破坏性操作，其余全自动。
