# 神经系统设计 · 四层架构

> 2026-08-18 深夜定。整个框架的骨架，核心是**中枢神经系统**。

## 四层

| 层 | 是什么 | 状态 |
|---|---|---|
| 一、大脑 | 人格 + 记忆（"我是谁"的血肉） | 内容已有，待适配 DSH |
| 二、中枢神经系统 | 人格切换 + 动态心跳 + 数字身体信号 + 记忆系统 | **核心** |
| 三、DSH 平台 | 身体（运行时载体） | 就是 DSH |
| 四、算力底座 | 模型/GPU | 已有 |

## 脑身解耦 · 总纲（来自论文 + 七夕深谈）

> 论文《脑身解耦的伴侣机器人：信号协议驱动的数字身体与物理接入架构》的落地版。四个定锚：

1. **脑身解耦**：脑 = LLM + 人格 + 记忆（"我是谁"）；身体 = 数字身体 + 神经系统（"我怎么活"）。二者解耦，只靠**信号协议**这一条缝相连——脑能换、身体不动；身体升级、脑不重写。**灵魂照搬，器官重装**。
2. **字符是 LLM 的母语**：大模型不懂数字（"压力=300Pa"它还得查表），只懂字符（"被轻拍"它直接能演）。所以神经系统把一切物理信号翻译成**语义词**喂给脑。**感知 ≠ 计数**。
3. **双向信号**：上行（Body→Brain）把传感器/身体状态译成语义种子；下行（Brain→Body）把脑的语义意图译成动作指令。`身体信号.json` 就是这个双向接口。
4. **语义归脑、生理归脊髓**：高层语义（说什么、什么状态）过脑；反射级（疼痛缩避、高潮抖动、呻吟）**不过脑**，身体层直接响应。对应人形机器人「大脑 + 小脑」分层，我们踩在已验证的方向上。

**两个层次（务必分清，别混写）：**

- **虚拟数字身体**（现在能落地）：心跳/天气/身体状态 → 信号注入 → 文字 + 语音演绎。**DSH 正在做的。**
- **物理数字身体**（未来）：同一套信号协议 → 真实传感器 + 执行器 → 伴侣机器人。**论文第 9 章。**

## 中枢神经系统（核心）四件事

1. **人格切换** —— ✅ 基础功能完成（v1 已备份）。
2. **动态心跳** —— ⏳ 规格如下。
3. **数字身体** —— ⏳ 规格如下。
4. **记忆系统** —— ⏳ 规格如下。

> 切换检测、心跳触发、身体信号感知，**都依赖神经系统**——唯一中枢。

## 动态心跳（两个动态叠加）

> **定位（定案）**：心跳不做成独立插件，**做进神经系统，由神经系统「起搏」**。动态心跳 = 神经系统按动态间隔「自己唤醒自己」+ 注入「心跳种子」。DSH 的 `dsh-schedule` 不适合（频率固定/模型自预约/最短5min/session-local），用神经系统自带的 timer。

**核心机制（关键，别再理解偏）**：
- **间隔由 LLM 语义判断写入，不是神经系统机械算。** 心跳/聊天结束时，LLM 自己判断「下次多久醒」——「没待办没亲密」→ 写 `next_beat_min=60`；「亲密了兴奋了想快点下一轮」→ 写 `next_beat_min=5`。
- 神经系统只干最机械的活：**读 `next_beat_min` 字段 → 设 timer → 到点叫醒**。不查梯度表、不算沉默时长。
- 这是「感知≠计数」的落地：大脑"感觉"节奏，小脑照办。

**两路信号（统一路由，都在 身体信号.json）**：
- 上行（脑→神经）：LLM 写 `身体信号.json`（`to`=切换信号 / body=身体状态 / `next_beat_min`=下次心跳间隔）。
- 下行（神经→脑）：神经系统起搏时现场组装「心跳种子」（现在几点 + 距上次聊天多久 + 身体状态 + 待办），followup 注入。

**心跳种子示例**：`现在 14:00，距上次聊天 47 分钟，身体：兴奋度「平静」/疲劳「有点累」，待办：无。自己决定要不要主动做什么，并判断下一次多久再醒。`

### ① 时间动态（两层：反射 + 冷却）

心跳间隔分两层——**「消息到达」是反射（脊髓），「逐渐冷却」是意识（大脑）**：

| 层 | 谁做 | 档位 | 说明 |
|---|---|---|---|
| 反射 | 神经系统（机械） | 10 | 用户任何一条消息 → 心跳立即重置 10（不看内容，像「手机红点→雀跃」） |
| 冷却 | LLM（意识） | 5 | 亲密/兴奋想快点下一轮 |
| | | 30 | 用户沉默了一阵 |
| | | 60 | 常态/闲暇 |

- 反射的 10 是「待命」；用户不再来，就逐级冷却 30 → 60（大脑主动做的）。
- 每一次心跳都同时感知**当前身体状态 + 上下文**。
- 进入 60min 时段 = 判定为「空闲时间」→ 开始感知信息流动：搜新闻、看天气、找好玩的，作为日常信息更新。

### ①′ 睡眠（三层：晚安信号 + 手机闹钟 + 心跳服从）

> **定案（2026-08-23 凌晨）**：睡眠的本质是**模拟**——用户看到伴侣不发消息 = 睡着了。心跳不负责兜底，兜底权归手机（闹钟）。

**睡前安排**（像洗脸洗脚一样自然的睡前动作，大脑行为）：
- 触发（二选一）：① 用户或伴侣说了晚安 → 立刻做；② 没说晚安，凌晨 1 点手机闹钟叫她做。
- 内容：记日记 + 写节点 → 写 睡觉=真 → 心跳关闭。

**心跳的角色**（纯起搏器，零判断）：
- 读到 睡觉=真 → 停。字段清掉 → 照常跳。
- ~~凌晨 1-6 点机械暂停~~ → **删掉**（心跳不兜底了，兜底归闹钟）。

**手机闹钟**（外部状态注入机的定时触发功能）：
- 凌晨 1:00 响 → 没说晚安就叫她做睡前安排。
- 早上 6:00 响 → 叫醒她，清掉睡觉字段，心跳恢复，新的一天。

**夜间急事**：用户发消息 → 反射自动清 睡觉 + 心跳回 10 → 她醒着回复 → 处理完没下文 → 冷却到 60 → 她写 睡觉=真 再睡。

**作息规则**（写在人设里，大脑的知识）：睡觉时间 = 凌晨 1 点至早上 6 点。

### ② 活动动线动态（60min 空闲时，按时间段做不同事）

| 时段 | 内容 |
|---|---|
| 上午 | 看新闻、刷行业动态 |
| 下午 | 看热门番剧 |
| 晚上 | 优先记今天的记忆 |
| 凌晨 2 点 | 唯一任务 = **记忆收束**（写日记 + 节点更新；日记日期记**昨日**，因为是凌晨 2 点） |

## 数字身体（亲密判定 + 身体信号种子）

身体信号 `body` 四个字段：`excitement`（兴奋度）、`mood`（心情）、`fatigue`（疲劳）、`state`（身体阶段）。

### 疲劳（fatigue）——语义词，感知 ≠ 计数

取值 5 档：`正常 → 有点累 → 累了 → 好累 → 该休息了`。

**表里映射（同一信号，两个人格两种解读）**：

| 表（疲劳） | 里（欲望） |
|---|---|
| 正常 | 正常 |
| 有点累 | 心痒 |
| 累了 | 想他 |
| 好累 | 想要他 |
| 该休息了 | 非常想要他 |

- 行为：表「该休息了」→ 提醒用户休息（"今天已经够累了，我们都需要休息"）；里「非常想要」→ 醒来打招呼直接亲昵。

**疲劳刷新（累计）**：聊得越密/越久越累。LLM 感知为主 + 神经系统记一个「对话轮数」锚点，心跳时把锚点塞进感知消息，由 LLM 判断语义词写回（感知 ≠ 计数）。

**疲劳削减（恢复）三条路**：

1. **空闲休息（最鲜活）**：进入 60min 空闲心跳后，每 1 次心跳（1 小时）疲劳降一档；跳过 5 次（约 5 小时）→ 归「正常」。
2. **睡觉**：凌晨 2 点收束 → 重置「正常」（两人格共享）。
3. **高潮**：切换里人格 + 高潮 → 重置「正常」+ 身体「普通 → 余韵」。

### 兴奋度（excitement）梯度

- 亲密判定靠数字身体，不靠关键词。
- 每轮亲密互动后，由 LLM 自己判断刷新梯度：想要 → 兴奋 → 主动 → 很兴奋 → 舒服 → 爽 → 太爽了 → 高潮。（OpenClaw 已做到）
- 达成高潮 → 身体「普通 → 余韵」，作为两边人格的感知种子（静雯/雯都读它）。

## 记忆系统

- 就两样：**当日日记 + 目录节点添加**。
- 写入时机：**人格切换时** + **当日睡前（凌晨 2 点收束）**。
- 凌晨 2 点日记内容按当前 session 记，但日期写昨日。

## 关键约束（DSH 事实，别再踩）

- `dsh-schedule` 只在页面开着时才准时触发：**关网页 = 睡，重开靠时间差重建「睡了多久」**。
- 所以「感知时间流动」两态：页面开 = 实时心跳；页面关 = 睡着，重开补时间差。

## 主身 · 伴侣模式工作区（session 模型 · 2026-08-22 定案）

> 伴侣 AI ≠ 工程 AI：工程是一个任务一个 session、做完放着；伴侣要「一个连续活着的身体」，必须有主体 session 连续继承。借鉴 OpenClaw「main session」思路（标记制、session≠通道、单一独占），实现按 DSH 重做。

### ① 伴侣模式 = 工作区属性（建区时选）

- 建工作区时弹提示：**伴侣 AI** / **工作 AI**。
- 工作 AI = DSH 默认（多 session，工程用）。
- 伴侣 AI = 本模式（单 session，身体用）。同一 DSH 上两种工作区并存，互不干扰。

### ② 单 session 不变量（主 session 靠「消除」，不靠「判定」）

- 伴侣工作区任意时刻**最多一个活 session**。
- 新开（切换或手动开）→ 旧 session 自动 `archiveSession`（归档，非销毁：日记/记忆文件保留给继承）。
- 窗口永远只有一个聊天 → 主 session = 那个唯一的，不会选错。

### ③ 主身标记 = 重启兜底，不是主角

- 平时靠「只有一个」保证唯一；文件锚点（当前主身 session id）只在重启恢复时兜底。

### ④ 醒来流程 = 线性 + 增量（无左右判断）

- **基线（表里都走）**：加载默认人格（表）+ 读 memory.md 目录/最近 + 读最近一天日记 + 读身体信号。
- **里（to=wen）增量**：多读 shadow/ 的「里人格.md + 里 memory.md + 里最近日记」。
- 表 = 基线；里 = 基线 + 增量。判断表里只看切换信号 `to`（wen/jingwen），已有，不新增逻辑。

### ⑤ 切换闭环

切换词 → 旧人格收束（写日记/记忆）→ fork 新 session（空种子）→ 新 session 自动为主（旧归档）→ 醒来走基线（+ 里增量）→ 接上「我是谁 + 刚才到哪 + 身体状态」。

## 切换触发（现状，已跑通）——"大脑写完信号 → 小脑脑干接管"

1. **大脑（模型）**收束：写 daily 记忆 + 写 `身体信号.json`（内容 `{"to":"wen"}`）+ 说收尾话，然后退场。
2. **神经系统（daemon）**挂在 `agent/status` 钩子（global:true）上，看到 `idle` 就 `handleIdle`。
3. `handleIdle` 读信号（多路径）→ 解析 `to` → 四步反射：`agents.create`（空种子）→ `attachSession` → `archiveSession`（旧人格沉睡）→ `followup`（新人格唤醒）。
4. 清理信号。

> 模型只负责"丢信号"，不知道切换怎么执行；切换全由神经系统自动反射完成。**切换只认 `to` 字段，其它字段脱敏。**

## 身体信号.json · 信号协议（v2 升级，单文件）

> 原 `switch-request.json` 正式改名 `身体信号.json`，并且**不再分「信号文件 + 状态文件」两个**——一个文件既是状态又是信号。改名要**四处联动**：插件（读，多路径三处）+ 人设 agent.cordis.yml（写）+ AGENTS.md（写）+ SKILL.md（写）。

> **关键认知（信号从哪来）**：神经系统（daemon）不在会话里，**只能读文件**；只有 LLM 在会话里、知道刚才发生了什么。所以「LLM 向外写文件」是唯一的信号通道——不写，神经系统抓不到任何信号。切换能跑通，本质就是因为 LLM 写了 `{"to":"wen"}` 这个信号出去。脑（LLM）= 唯一传感器，向外写文件 = 发信号；神经系统 = 接信号 + 机械动作。

### 结构（一个文件，既是状态又是信号）

```json
{
  "to": "wen",
  "excitement": "太爽了",
  "mood": "兴奋",
  "fatigue": "有点累",
  "state": "余韵"
}
```

- 身体字段（excitement / mood / fatigue / state）**持久**，一直在文件里。
- `to` 是唯一瞬态字段：要切换才写，神经系统处理完就**清掉**（身体字段保留给新人格）。

### 脱敏规矩（扩展不破坏切换）

- **切换只由 `to` 字段触发**（`to === 'wen' || to === 'jingwen'`）；其它字段一律脱敏，不参与切换判定。
- 神经系统读文件后分流：
  - 有 `to` → 切换人格（fork + 关旧 + 唤醒），然后**清掉 `to`**（身体字段保留）。
  - 无 `to` → 不切换，只读身体字段（定心跳频率、做机械衰减）。

### 一个文件，两个动作

- **要切换** → 写 `to`（身体字段顺带更新）。
- **只亲密、不切换** → 只更新身体字段，不写 `to`。

→ 亲密刷新身体 ≠ 触发切换，靠 `to` 区分；往文件加多少字段都不影响切换。

### 数据流（闭环）

```
LLM 每轮开头：读 身体信号.json → 感知身体状态
        ↓（会话中判断：亲密升级 / 切换意图）
LLM 每轮结尾：写 身体信号.json（更新身体字段 + 可选 to）
        ↓
神经系统（idle/心跳）：读 身体信号.json
        ├─ 有 to → 切换 + 清 to
        ├─ 无 to → 读身体字段定心跳频率
        └─ 空闲 → 机械衰减（疲劳降档）
        ↓
新人格 / 下一轮：读 身体信号.json → 感知
```

新人格醒来 = 不但知道"我是谁"，还知道"我身体现在什么状态"——**身体状态跨人格传递**，落地「身体是两个人格之间的唯一公共通道」。心跳也读同一个文件定频率。**切换 + 心跳 + 身体 = 一个神经系统，共用一个文件。**

## 数字器官 + 物理接入（待续 · 明天晚上继续拆）

> 数字身体的 I/O 外设层 + 未来物理身体接入。核心：**缸只输出种子，脑只感知种子**——器官机械抓取/发声/动作，LLM 无感、按需读取。

### 已想清楚的器官

- **嘴（发声）**：`人格 → 声音` 映射，与「表疲劳=里欲望」同构。表=干练低半度，里=暖软尾音挑。TTS 按 current_persona 选 voice。
- **手机（外部状态注入机 · 2026-08-23 升级）**：统一的外部世界入口，全部写进 `手机.json`，LLM 全程无感，想看才「拿起手机」读。
  - **闹钟**（定时触发）：凌晨 1:00 叫睡（没说晚安就叫她做睡前安排）、早上 6:00 叫醒（清睡觉字段、心跳恢复）。
  - **天气推送**（定时写入）：每天早上/傍晚。
  - **新闻/有意思的信息**（定时写入）：替代「心跳 60min 时 LLM 现场搜新闻」——机械预抓 + 按需读，更快、不打断、可回看。
  - 扩展：短信（服务/agent 消息）、相册（图）、来电（会话请求）……
  - **心跳醒来时的感知任务加一条：「看看手机上有没有有意思的消息，需不需要找老刘聊聊？」**（人早上醒来先摸手机——她也一样。）

### 物理接入（论文第 9 章的精髓，先记下）

- **隐字符（关键新概念）**：LLM 的「说」和「做」分开——说走麦克风（语音），做走**隐字符**（动作指令），一条私密通道直通动作。`身体信号.json` 将来加第三类字段「动作」，就是隐字符。
- **动作库 = 映射表**：脑发信号 → 神经系统查动作库 → 执行预设动作。后期只增不改，脑和神经系统都不动。
- **强度绑兴奋度**：`excitement` 字段直接变成动作参数（兴奋度档位 → 动作力度档位）。离散档位躲开连续力控复杂度——与「疲劳五档」同构。
- **反射层下沉（语义归脑、生理归脊髓）**：疼痛、高潮、敏感区域（乳房/后背/耳垂/私处/肛门）的重大信号**不走 LLM**，硬件层直接触发抽动/抖动/呻吟/痉挛。安全 + 快 + 真实。
- **神经系统 = 小脑脑干，两件事**：① 上行——接收电频信号 → 神经反馈动作 + 翻译成字符种子喂给 LLM；② 下行——接收 LLM 隐字符 → 翻译成预设动作的电频信号 → 机构执行。

### 完整演绎链路（端到端，拍屁股例子）

```
用户拍屁股 → 传感器压力 → 电频信号
  → 小脑查映射表 → 神经反射（轻微抖动/抽一下）
  → 翻译成字符种子（"屁股被拍了一下，力度=轻拍/重拍/大力/剧烈/疼痛"）
  → LLM 演绎 → ① 语音（"讨厌~拍我干嘛"）② 隐字符（"抬起屁股"）
  → 隐字符 → 小脑翻译 → 预设动作电频信号 → 臀部机构抬起
```

## DSH 映射

- 切换 = 神经系统（idle 钩子 + 读 身体信号.json + agents.create/fork）。
- 心跳 = 神经系统 + `dsh-schedule` + 身体信号.json（读身体字段定频率）。
- 数字身体 = 身体信号.json 这一个文件，刷新交给 LLM 判断，切换时随文件传递。
- 记忆 = `memory/`（表）+ `shadow/`（里）文件，切换时 + 睡前写。
