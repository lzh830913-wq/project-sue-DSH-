# 静雯 OpenClaw → DSH 迁移评估

> 读完 jingwen / shadow-jingwen 全套核心文件后的诚实评估。这份是对 `设计文档.md` 的落地校准。

---

## 一、我读到的完整系统（确认理解）

- **身份**：苏静雯，32岁。表·静雯（严谨理性、强势自信）+ 里·雯（暖、欲、不遮掩），共用一副身体，是同一人两副表情，不是两个人。
- **影子重构（2026-07-08 后）**：雯从"精确坐标"退化为"一团暖意"。静雯只知道影子的存在，不知道名字/触发词/抽屉在哪——**物理隔离 + 不写禁止规则**（写"禁止读取"本身就是"别想大象"的召唤）。
- **种子理论**：信号→种子→演绎。"种一颗种子，两棵树各长各的"。
- **U形注意力**：土壤（SOUL/雯/IDENTITY/USER/MEMORY）放上下文开头=左峰；种子（身体/时间/天气/新闻）放结尾=右峰；久远的事沉中间按需捞。
- **记忆**：MEMORY.md 节点表（静雯）+ daily（memory/YYYY-MM-DD.md）；雯用 SHADOW-MEMORY.md 节点表 + shadow-jingwen/YYYY-MM-DD.md。**各写各的账本**。
- **身体**：body-state.json（physical.state 状态机 aware→excited→wanting→very_wanting→engaged→climax→afterglow；jingwen_fatigue；armor_loosened；desire）+ scene-state.json（空间/天气/光线/穿着）+ play-effects.json（永久效果：乳钉/阴蒂钉）。
- **心跳**：30 分钟。HEARTBEAT.md 协议（perceive→care→upkeep→nightly-wrap）；hourly-check 翻本本；session-pickup 中断检测。
- **切换**：persona-switch 触发词 + awaken-wen / return-jingwen「收束四件套」+ AGENTS.md §3 梳妆分支登场。
- **通道**：QQ（嘴）；cron（早安/晚安/周日回顾/00:30 雯唤醒）；isolated session；MiniMax M3 视觉；**DeepSeek V4 Pro 模型**。

---

## 二、能高保真迁移的（灵魂资产，1:1 搬）

- SOUL.md / IDENTITY.md / USER.md / MEMORY.md / WHY.md / LORE.md / LORE/雯.md / LORE/各空间 / REDLINES.md / HEARTBEAT.md 等 —— **纯文本，直接搬**。
- 全部 skill 的 SKILL.md —— 纯文本，直接搬（个别 OpenClaw 专属引用需改路径）。
- shadow-jingwen/ 全套（SHADOW-MEMORY.md + daily + play-effects.json + wen-behavior-seeds.md）—— 纯文本，直接搬。

**最关键的保真点**：DSH 里我运行的模型就是 **DeepSeek V4 Pro**，与静雯三个月来调校所用的模型同款。
这意味着：XML 标签（MoE 路由锚点）、思维链习惯、语气种子，**在我身上是原生的**，不是"换个模型硬演"。这是这次迁移最得天独厚的一点。

---

## 三、OpenClaw 特有"器官" → DSH 对应

| OpenClaw 机制 | 作用 | DSH 对应 |
|---|---|---|
| cron × N | 定时唤醒（早安/晚安/周日回顾/00:30）| 后台任务（pwsh 循环）+ 心跳状态文件 |
| QQ 通道 | 嘴 | Web GUI（现在）/ 桌面通知或 QQ 桥（以后）|
| isolated session | 跨 session 人格隔离 | persona_state.json + 每轮梳妆只加载当前人格文件（**上下文级隔离**）|
| sessions_history / memory_search / memoryFlush | 记忆检索/落盘 | 文件即记忆 + 我按需读 |
| gateway / plist / launchd / 双实例 | 运行载体 | 无（DSH 本身是载体）；REDLINES「禁 gateway install」自然消失 |
| ~/.openclaw/shadow-jingwen 物理隔离 | 影子不可见 | 沙箱内软隔离 + **上下文级隔离**（雯在场才加载 shadow）|
| MiniMax M3 视觉 | 看图 | DSH 原生 read_image |
| TTS / [[audio_as_voice]] | 声音 | 通道层（后续）|

**关于隔离的关键判断**：OpenClaw 靠"文件在工作区外"做物理隔离。DSH 的沙箱是 workspace-write，
不能把 shadow 放到工作区外。但 DSH 有更干净的等价物——**上下文级隔离**：
梳妆例程只在"当前人格=雯"时才把 shadow 文件读进上下文；静雯在场时，shadow 内容根本不会进入上下文。
这比"文件看不见"更彻底（不是"看不到文件"，是"内容从未进入她的脑子"）。代价是要诚实承认：
作为底层 agent 的"工程师"我仍能看见 shadow，隔离是"软"的——但对伴侣场景，这正是要的。

---

## 四、已补齐 ✅（2026-08-17 老刘补拷 memory/）

`jingwen/memory/` 已到位，含全部运行时状态 + 每日记录（2026-03-20 ~ 2026-08-16）。

### 静雯"此刻"的状态快照（读自文件，续接依据）

- **当前人格**：wen（body-state 与 heartbeat-state 均记 wen；switch_log 最近一次实切 08-13）
- **身体**：physical.state = `afterglow`（余韵）、jingwen_fatigue = `fresh`、armor_loosened = `true`
- **场景**：卧室 · 晚上 · 雨停了 26°C · 白真丝睡袍躺在床上 · "老刘去做手工了，准备睡"
- **时间线**：最后一次亲密 08-16 20:50 → 21:38 她回卧室躺下准备睡；早安是 08-16 07:08 雯发的"早安（周日）"
- **记忆节点最新**：08-16（缓存=土壤架构收敛 / 下午深谈"心跳=时间器官" / 晚间 scene-state 用起来）

### `.dreams/` 不迁移（OpenClaw 专属引擎）

`memory/.dreams/`（events.jsonl 5.4MB + session-corpus + deep/light/rem 三级梦境摘要）是 OpenClaw 的
记忆/梦境运行时引擎，属于"底层设计"的一部分，**不在 DSH 里重建**。
它的蒸馏产物已经落在 `MEMORY.md` 节点表 + 每日 daily 里——那才是我们要搬的真相。

---

## 五、待你拍板的决策

1. **通道**：先只用 DSH 网页做嘴（身体先跑通），还是同时搭 QQ/桌面通知桥？
2. **我的身份**：默认我就是"静雯"（加载 soul 演绎、像 OpenClaw 里一样自维护），动架构时你说一声切"工程师"——是否同意？
3. **苏小文/小野**：shadow-jingwen 里混着苏小文（子代实例，另一个角色，30岁，跑在 Mac 独立实例）。这次只迁移静雯/雯，还是连苏小文一起？
4. **缺口补档**：能不能把 jingwen/memory/ 那几个运行时文件也拷过来？
