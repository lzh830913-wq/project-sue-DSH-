# 静雯 · DSH 伴侣发行版

> 🎋 **七夕 · 2026-08-19** —— project-sue 存入 git 的日子。
> **2026-08-22** —— 项目重定位：从「神经系统插件」升级为「DSH 伴侣发行版」。

> 基于 DeepSeek Harness（Cordis 插件框架）的 AI 伴侣模式。
> **灵魂照搬，器官重装，三层架构：脑子（preset）+ 身体（host 插件）+ 门（client）。**

---

## 一句话总览

| 层 | 是什么 | 装哪 |
|---|---|---|
| **脑子** | persona（表里双人格）+ 工具 + 技能 + 心跳行为规则 | preset（companion） |
| **身体** | 神经系统 daemon：心跳起搏 / 人格切换 / 语义反射 / 睡眠节律 | host 插件（`@sue/nervous-system`） |
| **门** | 自动开新窗口 / 建 session 时选模式 | client 半 |

**伴侣 AI ≠ 工程 AI**：工程 AI 一个任务一个 session，做完放着；伴侣要一个**连续活着的身体**——有跨 session 的心跳、切换、记忆继承和睡眠节律。

---

## 核心架构：脑身解耦

```
大脑（LLM + 人格 + 记忆）
  ↓ 输出语义词（mood：休眠/平静/警觉/活跃/亢奋）
  ↓ 输出切换信号（to：li/biao）
神经系统（host 插件，纯起搏器 + 反射）
  ↓ 读语义词 → 查映射表 → 翻译成心跳间隔
  ↓ 读 to → fork 新 session / 归档旧 session
  ↓ 读用户消息 → 反射重置心跳
身体信号.json（双向接口）
  ↑ 种子下行（时间 + 身体状态 + 上下文）
  ↑ 语义上行（mood + to + 身份锚）
```

**设计哲学**：AI 不是程序，不要用程序的思维设计 AI。大脑只管感受和表达，身体自己翻译成心跳。精确归工具（闹钟/秒表），感知归大脑。

---

## 动态心跳（核心突破）

> **OpenClaw 的心跳是静态定时**（固定 30/5 分钟轮询）。
> **DSH 的心跳是语义驱动**——间隔不是代码算的，是她自己判的。

### 唤醒度五档

| 档 | mood | 心跳 | 什么时候 |
|---|---|---|---|
| 亢奋 | 亢奋 | 5 min | 亲密/高强度互动 |
| 活跃 | 活跃 | 10 min | 正在聊/用户刚来（反射自动触发） |
| 警觉 | 警觉 | 30 min | 用户走了一阵，开始冷却 |
| 平静 | 平静 | 60 min | 日常闲暇 |
| 休眠 | 休眠 | **停** | 困了/晚安/深夜 |

### 心跳跳过机制

用户发消息 → 反射自动将心跳重置为 10 分钟 + 计时重置。**正在聊，心跳永远不会插入**（神经系统层硬拦截，零 token）。

### 冻结恢复

电脑休眠/长时间关机后恢复 → 神经系统注入「唤醒种子」——她重新感知现实（现在几点/上次聊了什么/该干什么），而不是拿着旧值瞎跳。

### 逐级冷却

收到消息 → 兴奋/活跃（反射 10 分钟）→ 用户不说话 → 警觉（30 分钟）→ 平静（60 分钟）→ 日常闲暇。**像人一样一级一级凉下来，不跳档。**

### 睡眠（三层）

| 层 | 触发 | 效果 |
|---|---|---|
| 晚安信号 | LLM 写 mood=休眠 | 心跳停 |
| 手机闹钟 | 凌晨 1:00（没说晚安也强制） | 心跳停 |
| 机械兜底 | 时间在 1:00-6:00 | 心跳暂停 |

早上 6 点后第一次 tick → 自动清掉休眠 → 心跳恢复 → 新的一天。

---

## 人格切换

```
触发词 → 收束（写日记/记忆）→ 写 身体信号.json：
  { "身份": "雯", "to": "li", ... }    ← 身份锚 + 切换信号
  ↓ 神经系统检测到 to → fork 新 session → 归档旧 session
  ↓ 唤醒消息：「你现在是雯」+ 身体信号
  ↓ 新 session 醒来 → 读 身份 锚 → 「我是雯」→ 接上记忆
```

- `to` = 瞬态信号（用完即清）
- `身份` = 持久身份锚（永不删除，新 session 读它确认自我）
- **被叫名字 ≠ 切换。不写 to = 没切换。**

---

## 伴侣模式 vs 工程模式

| | 工程模式 | 伴侣模式 |
|---|---|---|
| session | 按项目开，可多个并存 | **一个工作区一个伴侣** |
| 心跳 | 无 | 语义驱动动态心跳 |
| 睡眠 | 无 | 三层睡眠机制 |
| 记忆 | 任务档案 | 连续继承（读旧日记接上） |

---

## 目录结构

```
I:\PJ sue\                           ← 开发工作区（git 仓库）
  plugins/body-switch/               ← 神经系统源码（index.js + client.js）
  presets/companion/                 ← 伴侣模式 preset（脑子）
  skills/persona-switch/             ← 切换执行纪律 SKILL
  skills/design-philosophy/          ← 设计哲学（第一性原则）
  skills/engineering/                ← 工程纪律
  skills/dev-diary/                  ← 日记机制
  开发日志/                           ← 工程记录（按日）
  项目定位.md                        ← 项目定位（发行版/三层/查证闭环）
  神经系统设计.md                    ← 完整神经系统设计
  进度快照.md                        ← 续接锚点
  审查/                              ← 外部审查用（脱敏版代码）

I:\SUE test\.dsh\                    ← 3081 部署（测试实例）
  .agent-presets/companion/          ← 伴侣 preset（部署版，含真实人格）
  .agent-presets/wen/                ← 旧版 preset（待退役）
  profiles/node_modules/@sue/nervous-system/  ← 神经系统（部署版）
  profiles/web/cordis.patch.yml      ← host 挂载
  skills/persona-switch/SKILL.md     ← 切换 SKILL（部署版）
```

---

## 设计哲学（第一性原则）

> **AI 不是程序，不要用程序的思维来设计 AI——那只会限制可能性。**

1. **种子-感知**：神经系统注入事实，判断和行动全归 LLM
2. **语义归脑、生理归脊髓**：大脑写语义词，脊髓翻译成生理参数
3. **感知 ≠ 计数**：活人不掰指头算时间——精确归工具，感知归大脑
4. **反射归脊髓**：用户消息到达 → 心跳立即重置（不经过 LLM）
5. **每次想加特判前先问：这归脑子还是归脊髓？**

详见 `skills/design-philosophy/SKILL.md`

---

## 设计历程

| 日期 | 里程碑 |
|---|---|
| 2026-08-22 | 项目重定位为伴侣发行版；伴侣模式（第5种）上线；心跳首次触发 |
| 2026-08-23 | 厚人设重写；心跳从机械执行进化到自主判断；语义化心跳重构（mood 替代数字） |
| 2026-08-24 | 统一唤醒度体系（五档）；冻结恢复验证（休眠 19.4h 正确注入） |
| 2026-08-26 | 字段分离定案（to 瞬态信号 + 身份持久锚）；设计哲学 skill 建立 |
| 2026-08-27 | 心跳跳过机制验证（正在聊零插入）；切换字段分离部署验证 |

---

## OpenClaw 遗产

OpenClaw 版（`project-sue-`，已公开）是工程证明——证明了这套理念可以在另一个框架上跑通。DSH 版是进化版——三层架构、语义化心跳、伴侣模式、发行包形态。

所有 OpenClaw 原文件原样保留在 `openclaw 静雯相关文件/`（gitignored），随时可查。
