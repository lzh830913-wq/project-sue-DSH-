# 人格动态化 · v2 设计笔记

> 2026-08-18 深夜定方向。v1（双向切换全通）已收。v2 目标：把写死的「人格名 / 切换信号 / 触发词」动态化，配一个可编辑的「人格设定」界面。

## 核心判断（老刘）

- **人格内容不产于切换**：性格、记忆、身体状态由人格本体承载，本来就与切换机制解耦，**切换机制不需要去管它们**。
- ⚠️ **纯 DSH 下人格本体在哪**（别把 OpenClaw 结构当地基）：3081 纯 DSH 实例只有 `preset 人设文本（agent.cordis.yml）+ AGENTS.md + memory/ + shadow/`，**没有 SOUL.md / 雯.md / LORE**。`soul/SOUL.md`、`soul/LORE/雯.md`、`boot.md`、`plugins.json`、`plugins/` 都是 OpenClaw 迁移遗留（在 `I:\PJ sue`），纯 DSH 不加载它们。
- 真正「写死」在切换机制里的只有三样，需要动态化：
  1. **人格名**：表=静雯 / 里=雯，写死在插件、人设、AGENTS.md、技能里。
  2. **切换信号值**：`"wen"` / `"jingwen"`，写死在插件判方向 + 人设写信号里。
  3. **触发词**：`把盔甲脱了` / `回来吧` 等，写死在技能 + 人设里。

## 方案草图

### 1. 配置源

`persona-config.json`（或 DSH settings），一个对象管完：

```json
{
  "outer": { "id": "jingwen", "name": "静雯" },
  "inner": { "id": "wen", "name": "雯" },
  "triggers": {
    "outer_to_inner": ["把盔甲脱了", "今晚不需要盔甲", "让我看看真正的你", "我要你", "想你了", "雯儿~"],
    "inner_to_outer": ["回来吧", "静雯，回来", "切换回来", "好了", "不早了", "让静雯回来吧"]
  }
}
```

### 2. 信号值用稳定 id，不用显示名

- 信号内容写 **id**（`"jingwen"` / `"wen"`），不是显示名。
- 神经系统读配置，用 `signal.to` 匹配 `outer.id` / `inner.id` 判方向；唤醒语里用 `config[target].name` 当显示名。
- 这样用户改名（如 里→"小狐狸"）不影响切换判向，只影响人设/UI 里的称呼。

### 3. 人设动态渲染（改名生效的关键）

- 现在名字 + 触发词是**写死在人设文本里**的。只改配置文件不够——模型看到的 system prompt 还是"雯"。
- 需由插件把配置动态注入 prompt：persona 段用名字、触发词清单从配置读。
- AGENTS.md / persona-switch 技能同样要去掉写死的名字和触发词，改成「见配置 / 由插件注入」。

### 4. UI 插件（人格设定）

- 一个 settings section：编辑 表/里 的**名字** + 各自的**触发词**。
- v2 先做「名字 + 触发词」；性格等是 preset 人设文本 + AGENTS.md，本就可编辑，界面化是更后面的事。

## 关键点（别踩）

1. **显示名 vs 稳定 id 分离**：信号/id 用稳定值，显示名只进人设和 UI。
2. **人设要动态注入名字 + 触发词**——这是「改名生效」的核心，只改配置文件不够。
3. 触发词要能映射到**方向**（outer→inner / inner→outer），不是平铺一堆词。

## 落地顺序（建议）

1. 抽出 `persona-config.json` + 神经系统读配置判方向（去硬编码）。
2. 人设/AGENTS/技能改成引用配置（动态名字 + 触发词）。
3. UI 插件做「人格设定」编辑名字 + 触发词。
4. **OpenClaw 遗留处置**（先想清楚，再动手）：决定 `I:\PJ sue` 里那套 OpenClaw 结构（SOUL.md / 雯.md / LORE / boot.md / plugins.json / plugins/）是「迁进纯 DSH 的 preset 人设文本 + AGENTS + memory/shadow」还是「降级为参考资料」，去掉双轨。别在 v2 里默认 soul 结构可用。
