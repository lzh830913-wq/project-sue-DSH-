# 项目定位 · DSH 伴侣模式（伴侣发行版）

> 2026-08-22 定案。这不是「设计一个插件」，是整个项目的重定位：从「神经系统插件」升级为「基于 DSH 的伴侣发行版」。

## 一句话定位

**DSH（原封不动）+ 伴侣层（身体 host 插件 + 脑子 preset + 门 client）= DSH 伴侣模式。**

- 不改 DSH 源码（**不是 fork**）。
- 不进「插件市场」（DSH 也没有这东西）。
- 是「**加层**」：利用 DSH 现有扩展点，在上面加一层伴侣能力。

## 最终流程（用户实测确认）

1. **打开（新建）会话**。
2. **同一个界面里一起选「模式 + 工作区」**：模式 = 选「伴侣」preset；工作区 = 选/建目录，名字自定。
3. **开始对话** → host 神经系统读到 `composedPreset='companion'` → 判定伴侣区 → 单 session 启动。

> 模式和工作区是**一起选**的（DSH 新建会话界面两者并排），不是「先工作区、后建 session 选模式」两个动作。

## 本质区别：伴侣 AI ≠ 工程 AI

| | 工程 AI | 伴侣 AI |
|---|---|---|
| session | 一个任务一个，做完放着 | 一个连续活着的「身体」 |
| 主次 | 无所谓，每个都是完整工作流档案 | 必须有主体 session |
| 连续性 | 不需要继承 | 必须连续继承（读旧日记/记忆接上） |
| 跨 session | 无（session 之间没关系） | 有（身体跨 session：切换/心跳/记忆） |

**工程 AI 天生「preset 就够」（每个 session 自足）；伴侣 AI 天生需要「跨 session 的身体」（host daemon）。**

## DSH 架构现实（两层铁律）

| 平面 | 装什么 | 份数 |
|---|---|---|
| host 组合 | 跨 session：注册表、持久化、sandbox、模型路由、会话路由 | 进程一份 |
| agent preset | 单 session：人设、工具、技能、prompt 段 | 每 session 一份 |

铁律：**「一个服务有 agent 平面之外的消费者，就不能进 preset」**——神经系统的消费者是所有 session 的切换/心跳，所以它必须 host-side。

## 伴侣模式 = 三层

| 层 | 是什么 | 装哪 |
|---|---|---|
| 脑子 | persona（表里）+ 工具 + 技能 + 切换规则 | preset（= 模式本体） |
| 身体 | 神经系统 daemon（fork/归档/心跳/读信号） | host 插件（`@sue/nervous-system`） |
| 门 | 自动开新窗口（`sessions.open`）；「伴侣」模式出现在新建会话的模式选择里 | client 半（`@sue/nervous-system` client.js） |

## 发行包：身体一个插件装到底

- **不是「市场」，是「发行包」**：DSH 没有应用商店；扩展 = npm 包 + preset 目录 + host patch。别人装齐这三样 = 获得完整伴侣功能，不需要我们的「二次版本」（因为没 fork）。
- **身体 = 单一 host 插件 `@sue/nervous-system`，收容所有跨 session 功能**：切换（fork/归档）、心跳、单 session 不变量、记忆继承、将来 QQ/短信路由，全进这一个插件。
- **脑子不进身体**：persona、工具、技能、记忆内容 → preset。

| 功能 | 进哪 |
|---|---|
| 人格切换 / 单 session / 心跳 / 记忆继承 / 未来通道 | 身体（神经系统 host 插件） |
| persona / 工具 / 技能 / 记忆内容 | 脑子（companion preset） |

## 关键决策（本轮定案）

### 1. 伴侣模式 = 一个 preset（模式），新建会话界面与工作区一起选 + 单 session 由 host 执行

- 「伴侣」做成第 5 种 preset（和内置 4 种平级）。**实测确认**：打开（新建）会话时，「模式」和「工作区」在同一个界面里**一起选**。
- 工作区（workspace）无 mode 字段（`workspace.create` 只收 `path`）；「单 session 不变量」由 host 神经系统执行：判当前 session 挂的是伴侣 preset（`composedPreset(agent.ctx)==='companion'`）→ 进入单 session 模式（新 session 自动归档旧的）。
- 体验：打开会话 → 模式+工作区一起选 → 对话 → 此后该工作区永远只有一个聊天。

### 2. 单 session 不变量（主 session 靠「消除」，不靠「判定」）

- 伴侣工作区任意时刻**最多一个活 session**。
- 新开（切换或手动开）→ 旧 session 自动 `archiveSession`（归档，非销毁：日记/记忆文件保留给继承）。
- 窗口永远只有一个 → 主 session = 那个唯一的，不会选错。

### 3. 主身标记 = 重启兜底，不是主角

- 平时靠「只有一个」保证唯一；文件锚点（当前主身 session id）只在重启恢复时兜底。

### 4. 醒来流程 = 线性 + 增量（无左右判断）

- **基线（表里都走）**：默认人格（表）+ 读 memory.md 目录/最近 + 读最近一天日记 + 读身体信号。
- **里（to=wen）增量**：shadow/ 的「里人格.md + 里 memory.md + 里最近日记」。
- 表 = 基线；里 = 基线 + 增量。判断表里只看切换信号 `to`（wen/jingwen），已有，不新增逻辑。

### 5. 切换闭环

切换词 → 旧人格收束（写日记/记忆）→ fork 新 session（空种子）→ 新 session 自动为主（旧归档）→ 醒来走基线（+ 里增量）→ 接上「我是谁 + 刚才到哪 + 身体状态」。

## 借鉴 OpenClaw（思路，不是源码）

1. **session ≠ 通道**（脑子 ≠ 嘴巴）。
2. **main session = 唯一完整大脑**（QQ/心跳/cron 全挂它）。
3. **「main」是标记，不是固定号**：`dmScope:"main"` / `sessionKey:"agent:main:main"`——Key 稳定、ID 会变。
4. **单一独占，不做分身**。
5. **醒来增量**：表读基线，里在基线上多读 shadow 三样。

## 已查证结论（2026-08-22）

- ✅ **能加第 5 种模式**：preset 目录 + `preset.yml`，选择器自动出现（原生）。
- ✅ **「模式」= preset，和「工作区」在新建会话界面一起选**（`conversation.hero.agentPreset` 芯片 + 工作区选择器并排）。
- ✅ **工作区无 mode 字段**（`workspace.create` 只收 `{path}`）→ 「单 session」由 host 神经系统执行，不靠建区弹窗。
- ⚠️ **建区弹窗可重写但不必**（`directoryFlow` slot 是 single、shadows-shipped-ui，重写成本高且与「session 选 preset」重复）→ 放弃重写，用「preset + host 判 preset」实现同等体验。
- ✅ **host 能读「session 挂的哪个 preset」**：`ctx.agentPresets.composedPreset(agent.ctx)` 返回 preset id（从活 scope 链读，切换代码已在用）。判伴侣区 = `composedPreset(agent.ctx) === 'companion'`。
- ✅ **bonus**：`composeFrom(agentCtx, parentCtx)` 让 fork 子 session 直接绑定父 session 的**同一份组合（同一代，不重读 roster）**——比现在 `mount(resolvedId)` 更适合「切换 = 同一个人」。

## 与现有文档的关系

- 详细 session 模型 → `神经系统设计.md`「主身 · 伴侣模式工作区」节。
- 心跳 / 数字身体 / 记忆 → `神经系统设计.md`。
- 人格动态化 → `人格动态化.md`。
- 开发模式分工 → `开发模式.md`。
