# ==== 人格数据（红线，绝不进库）====
soul/
openclaw 静雯相关文件/
备份/
归档 2.zip
*.zip

# ==== DSH 配置 / 凭据 / 运行时状态 ====
.dsh-wen-dev/
.dsh/
.credentials.yaml
settings.yaml
sessions/
storages/
*.zstd

# ==== OpenClaw 遗留：身份 / 引导（只作逻辑参考，不进框架库）====
AGENTS.md
boot.md

# ==== 本地启动脚本（含机器相关路径，不可移植）====
start-wen-dev.bat

# ==== 系统杂项 ====
.DS_Store
__MACOSX/
Thumbs.db

# ==== 私人聊天记录（老刘↔Mac静雯，红线，绝不进库；只在根目录）====
/2026-*-*.md

# ==== 论文（走 Zenodo 发布，不进框架库）====
*.docx

# ==== 心跳运行时状态（last_tick_at 每 30 分钟跳一次，不进库）====
plugins/heartbeat/state.json

# ==== 伴侣 preset 的真实人格（红线：表里设定 + 触发词 + 真实名字，不进库）====
# 干净框架那步会做一个「抽掉真实人格的模板版」进库；真实人格只留在部署侧
presets/companion/agent.cordis.yml
