# 进度快照 · 续接锚点

> 2026-08-19。**双向切换全通，UI 层三堵墙全拆完**（归组 + 关旧 + 界面自动切换）。
> v2 step1（信号文件改名 `身体信号.json`）✅ 已验证；v2 step2（心跳）✅ 已部署 3081，待验证心跳实际触发。
> git 库已建并推送：`github.com/lzh830913-wq/project-sue-DSH-`（框架+源码进库，人格数据已 gitignore）。
>
> **2026-08-22 项目重定位**：这不是「插件」，是「DSH 伴侣发行版」——伴侣 AI 有跨 session 的「身体」（host daemon），天生比 coding 模式多一层（preset + host + client）。**接活先读 `项目定位.md`。**

## 🎯 地基开发计划（2026-08-22 · 当前主线）

**心跳卡住的根因**：没有「单 session」概念 → 不知道谁是发/收信号的源头。**解法：先做「模式」这块地基，再做心跳。**

**最终流程（用户实测确认）**：
1. 打开（新建）会话 → **同一界面里一起选「模式 + 工作区」**（模式=选「伴侣」preset；工作区=选/建目录，名字自定）。
2. 开始对话 → host 神经系统读 `composedPreset='companion'` → 判伴侣区 → 单 session。

**地基开发顺序（两件事）**：
1. **建「伴侣 preset」**：把 3081 的 wen preset 复制成 companion preset（preset.yml 写名字/描述）。
2. **改神经系统（身体）**：判 preset（`composedPreset==='companion'`）+ 单 session（新 session 自动归档旧）+ 醒来基线读取（记忆/日记/身体信号 + 里增量）。

> 全部查证已闭环，详见 `项目定位.md`。

## 🎯 当前状态（2026-09-01 · 最新）

**心跳系统完成语义化重构 + 五档唤醒度 + 感知式睡眠。休眠语义已定论并全系统统一。** 详见 `神经系统设计.md` + `开发日志/2026-08-23.md` + `开发日志/2026-08-24.md` + `开发日志/2026-09-01.md`。

**休眠语义定论（2026-09-01）**：**休眠 = 睡眠长心跳（480 分钟一觉），不是「心跳停」**。睡够 8 小时自然醒一次，感知时间戳看一眼现实，自己决定继续睡还是醒来。分层认知：**tick 是 DSH 的时间系统，心跳是 tick 之上的「激活」**——不需要特判「心跳停」，让休眠走长心跳就自然了（删掉了 -1 哨兵和 `mood==='休眠' return` 特判）。

**已验证 ✅**：
- 语义化心跳（mood 映射表驱动，LLM 只写语义词不写数字）
- 心跳跳过（正在聊零插入，反射重置 lastBeatAt）
- 逐级冷却（她自主降档 10→30→60）
- 身份锚（新会话醒来读 身体信号.json 的 身份 字段 → 知道自己是雯）
- 切换字段分离（身份+to 双字段，两次写入，身份先于 to）
- 晚安睡眠闭环（说晚安→bedtime-routine（日记→节点→休眠信号）→晚安→长心跳睡眠；老刘来→消息反射唤醒）
- 冻结恢复（休眠19.4h→唤醒种子正确注入）
- 切换后不补跳 ✅（2026-09-01 代码确认：切换完成 lastBeatAt 重置 + switching 期间 tick 跳过）

**待验证 ⏳**：
- bedtime-routine 完整流程实测（今晚道晚安走一遍；人设/SKILL 改动需**新开会话**才生效）
- time-context 时间感知实测（2026-09-01 已配进 companion preset：每轮自动注入当前时间+流逝时长——心跳 prompt 里「消息里有时间戳」从此有机制兜底）

**待开发**：
- 伴侣工作区隔离（提醒制：选工作区时提醒「建议独立空间」+ 检测已有伴侣询问「新建还是继续」）
- **伴侣 UI 定制**（IDE→IM 感，产品级大方向，老刘在想）
- 手机插件（闹钟+推送，外部状态注入机）
- 干净框架（preset 模板化进 git——当前 agent.cordis.yml 是 gitignored 红线）
- 并发写入优化（神经系统反射和 LLM 竞争写同一文件）
- 内心独白 → 隐输出（前后台分离）

**设计哲学**（必读）：
- 最高原则：**AI 不是程序，不要用程序的思维来设计 AI**
- 种子-感知：神经系统给事实，判断归 LLM
- 语义归脑、生理归脊髓：LLM 写语义词，脊髓翻译成生理参数
- 感知≠计数：活人看一眼钟就够了，精确归工具
- 详见 `skills/design-philosophy/SKILL.md`（含违反案例存档）

**已知教训（别再踩）**：
- host 插件监听 agent/status 事件不加 {global:true}
- 全角空格（U+3000）会炸 YAML `text: |-` 块
- 不要用魔法数字（-1）做哨兵值——需要特殊行为就写明确的条件判断
- 人设改动必须新开会话才生效（旧 session 挂的是旧人设）
- 提权审批可能等很久——提醒老刘盯着点

**关键认知**：心跳不写的根因是人格薄，不是机制。土壤厚了自主判断就来。

## ✅ v1 完成节点（先读这里 · 改 3081 前必看）

- **3081 双向切换 v1 已跑通，已备份**：`I:\PJ sue\备份\3081-switch-v1-20260818\`（7 个文件 + 恢复说明）。
- **改 3081 前，先确认这份备份在**，别把能跑通的版本修坏。还原步骤见备份目录里的 `恢复说明.md`。
- 源码档同步在 `I:\PJ sue\plugins\body-switch\`（index.js / client.js / package.json）。

## ✅ git 仓库（2026-08-19 建）

- 远程：`github.com/lzh830913-wq/project-sue-DSH-`，分支 `main`，本地 `I:\PJ sue\.git`（已设 `origin`）。
- 代理：git 走 `socks5h://127.0.0.1:8888`（SSR，写在本仓库 `.git/config`）；token 已嵌入 remote URL（2026-09-01，存 `.git/config` 本地文件，不进库）。push 若再 401/403 = token 失效，重新申请后 `git remote set-url origin https://<新token>@github.com/lzh830913-wq/project-sue-DSH-.git`。
- 进库：`plugins/body-switch/`（神经系统源码）+ 全部设计文档 + 器官 `PLUGIN.md`。
- 不进库（`.gitignore`）：`soul/`、`openclaw 静雯相关文件/`、`备份/`、`.dsh-wen-dev/`、`AGENTS.md`、`boot.md`、`start-wen-dev.bat`、`归档 2.zip`。
- 常用：改完 `git add -A && git commit -m "..."`；推送 `git push`。
- ⚠️ 待办：`plugins/heartbeat/state.json` 是运行态、有未提交改动，建议 gitignore 掉或 commit 一次。
- ⚠️ 待办（明天）：**仓库重构为「干净框架」**——现在 git 是把整个 `I:\PJ sue` 工作区当仓库（源码+设计文档混一起），不是干净框架。该按 3081 部署结构归位：`nervous-system/`（源码）+ `preset/agent.cordis.yml`（模板化，抽掉真实人格）+ `skills/persona-switch/` + `profiles/web/cordis.patch.yml` + `docs/`。注意 `agent.cordis.yml` / SKILL.md / cordis.patch.yml 目前只在 3081 和 `.dsh-wen-dev`（被 gitignore），**没进 git**，缺了拼不齐能跑的系统。

## 新会话怎么接（先读这里）

新开会话后，直接对 agent 说：**"读 `I:\PJ sue\进度快照.md` 和 `I:\PJ sue\README.md`，接着昨天的进度继续。"**
就这一句，它就能接上。设计细节看 `项目定位.md` / `神经系统设计.md`；**工程全过程看 `开发日志/`（怎么做、为什么，越详细越好）**。

## ✅ 已达成（今日大里程碑）

**神经系统（`nervous-system` 插件）后端完整闭环，双向切换跑通：**
「把盔甲脱了」→ 静雯收束写信号 → fork → 雯醒来；「回来吧」→ 雯收束写信号 → fork → 静雯醒来。

## 关键技术点（踩出来的，别再踩）

1. **idle 钩子**：`ctx.on('agent/status', ({agent,status})=>..., { global: true })` —— 必须加 `{global:true}`，payload 是 `{agent,status}`（不是 `{status}`）。
2. **上下文隔离**：fork 时 `seed: []`（空种子），记忆靠文件传递，不塞历史。
3. **挂 preset**：`ctx.agents.create({..., agentPreset: resolvedId, setup: (agentCtx)=>ctx.agentPresets.mount(agentCtx, resolvedId)})` —— 否则子 agent 用部署默认人设（`{{model}}` 没值）。
4. **继承模型**：`agentOptions: {provider, model}` 从父 agent 的 `requestHeader().config` 读。
5. **唤醒**：直接 `childAgent.followup(createUserMessage({content:[{type:'text',text:...}], source:{kind:'plugin',plugin:'nervous-system'}}))` —— 不走 schedule/change 事件（那不会触发）。

## 文件位置（测试实例 I:\SUE test\.dsh）

- 插件：`profiles/node_modules/@sue/nervous-system/{index.js,client.js,package.json}`（源码在 `I:\PJ sue\plugins\body-switch\`）
- 挂载：`profiles/web/cordis.patch.yml`（`name: '@sue/nervous-system'`，双面包）
- 人设：`.agent-presets/wen/agent.cordis.yml`（persona + 切换硬规则）
- 技能：`skills/persona-switch/SKILL.md` + 工作区 `I:\SUE test\AGENTS.md`
- 启动：双击 `I:\PJ sue\start-wen-dev.bat`

## ✅ 已达成（深夜续 · UI 层全闭环）

三堵墙全部拆掉，双向切换跑通：归组（attachSession）+ 关旧（archiveSession）+ 界面自动切换（client half `sessions.open`）。

### 踩过的坑（按时间顺序，别再踩）

| # | 现象 | 根因 | 解药 |
|---|---|---|---|
| 1 | 子 session「未分组」 | `agents.create` 不入 workspace `sessionIds` 记账 | `resolveByPath(cwd)→attachSession(childId)` |
| 2 | 旧会话不自动关 | 没人归档 | `archiveSession(oldId)` |
| 3 | 界面不自动切换 | 缺 client `open(childId)` | 双面包 client half 订阅 `sessions.list` 自动 `open` |
| 4 | 信号读不到 | 模型把 `switch-request.json` 写进 `memory/`，插件只读根目录 | 插件多路径探测（根/memory/shadow） |
| 5 | 雯原地演静雯 | 测试实例 `AGENTS.md` 旧版「切回静雯」原地切换指令 + preset「写信号」打架 | 删掉 AGENTS.md 旧指令，统一走写信号 |
| 6 | 静雯醒来卡死 | 唤醒语「按梳妆规则醒来」，测试实例没 boot.md，静雯瞎翻文件撞 sandbox | 唤醒语改直白「你现在是X，给老刘打个招呼」 |
| 7 | 雯写文件弹两次沙箱批准 | MiniMax-M3 写文件乱加 `sandbox_permissions` 自己升权 | AGENTS.md 加禁令「写文件别填 sandbox_permissions」 |
| 8 | 雯只念「我收好了」不写信号（假切换） | 模型把收尾话当成切换本身 | 人设+技能改成「切换靠写文件，没写文件=没切换」（**仍偶发**） |

## 剩最后一点（雯「耍赖」打磨）

雯偶尔还假切换（只念收尾话不写 `身体信号.json`），需要老刘再戳一句才补上。prompt 已尽力，若还要更硬：
- **A（工具方案）**：给 agent 注册 `persona_switch` 工具（`to: wen/jingwen`），execute 里写信号文件——工具调用比「念台词写文件」更结构化，难糊弄。
- **B（去模型依赖）**：神经系统自己监听收尾话文本 / 触发词，不靠模型写文件。改动更大，但最彻底。

## 下一步（v2 · 人格动态化）

方向已定（老刘）：把写死的「人格名 / 切换信号 / 触发词」动态化，配可编辑的「人格设定」界面。
详见 `I:\PJ sue\人格动态化.md`（核心：显示名 vs 稳定 id 分离；人设要动态注入名字+触发词；人格内容本就在 soul 文件里，切换只管名字+信号+触发词）。
